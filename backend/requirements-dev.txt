# Test dependencies — install with: pip install -r requirements-dev.txt
# (after installing requirements.txt first)

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # required by fastapi.testclient (already in requirements.txt)
trustme>=1.1         # ephemeral CA/server certs for the TLS-pinning SSRF tests
